compute_outside_share <- function(mkt_shares, market_id, data_frame) {
    # expect mkt_shares = c("something")
    # market_id = c("something", "something2")
    # df <- data_frame
    #
    # df %>%
    #     group_by(year, country) %>%
    #     mutate(inside_share = sum(mkt_share)) %>%
    #     mutate(outside_share = 1-inside_share)


}
